### libpng-android-builder

A simple shell script to cross-compile libpng project for Android targets.

Builds the binaries and libs using static linking.

Typical usage:
```
bash ./build.sh
```

Requirements:
- Android SDK & NDK
- prebuilt zlib (via zlib-android-builder)
- some dev tools
